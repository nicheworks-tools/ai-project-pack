# Model Selection

- Use `../adapters/chatgpt.md` when ambiguity needs to shrink before edits.
- Use `../adapters/claude.md` when evidence quality matters most.
- Use `../adapters/codex.md` when the repo itself needs careful edits.
- If unsure, start with report-only regardless of model.
