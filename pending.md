# Pending

Only unresolved items that materially affect next passes belong here.

## Open
- Define a cadence and owner model for periodic validation runs (for example: per merge, daily, or weekly).
- Decide whether future validation should enforce minimum section headings in core files to catch structural regressions.

## Waiting on external input
- Collect first external user feedback from at least two independent first-run attempts before tightening adapter language.

## Resume conditions
- Resume adapter wording updates after real user friction points are documented in `updates/` and linked from `sources.md`.
- Resume validator strictness decisions after at least one month of update logs shows recurring failure patterns.
