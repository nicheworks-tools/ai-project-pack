# Next Actions

Concrete, startable actions only.

## Now
- Run one first-time-user dry run using only `README.md` and `AGENTS.md`; capture friction points in a new dated file under `updates/`.
- Add one `sources.md` entry that cites the dry-run evidence once captured.
- Perform one handoff simulation between two different models and record restart time and misses.

## Soon
- Tighten adapter examples based on observed dry-run friction (without changing operating contract).
- Add one weekly-review update log that explicitly checks alignment between `current-truth.md`, `pending.md`, and `next-actions.md`.

## Later
- Evaluate whether validator should fail on missing required section headings in key docs.
- Add one optional template for handoff-specific update entries if repeated patterns justify it.
